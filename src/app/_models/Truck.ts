export interface Truck {
    truckCode: string;
    firstName: string;
    lastName: string;
    status: string;
    personalLeave: boolean;
    sickLeave: boolean;
    idleTime: number;
    color: string;
}
